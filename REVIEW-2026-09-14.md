# Review 2026-09-14 — 连接便利性专项

> 背景：两人跨网通话目前依赖邀请码二段式手动交换，体验差；且手边没有信令服务器。
> 参考项目：tl-open-source/tl-rtc-file、miroslavpejic85/mirotalk。

## 1. Git 状态：不是最新

| 项 | 状态 |
|---|---|
| 远端 | `origin/main = 00f63c6`（"新增 PWA 支持与安卓 TWA 打包方案"），本地**落后 1 个提交** |
| 本地 | `HEAD = d978ec9`，另有 **14 个文件未提交修改**（+331/-118，含 call.ts / invite.ts / network.ts 等） |
| 同步建议 | 先 `git stash` → `git pull --ff-only` → `git stash pop`，解决 PWA 提交与本地改动的潜在冲突后再提交 |

## 2. 开发规范要点（AGENTS.md 硬约束，改动前必查）

- C2 exe ≤ 13 MB；C4 新增磁盘 ≤ 1 GB；C7 所有新功能必须可导出诊断日志。
- node_modules 仅限 frontend/，禁止新增 UI 框架；禁止 CGO、禁止新运行时。
- 安全：媒体强制 DTLS-SRTP；**SAS 核对码兜底信令中间人**（这一条恰好是启用第三方公共信令的安全前提）。
- 信令三模式共用一套 `SignalMessage` JSON；连接失败必须给可读分类提示。

## 3. 代码 Review 发现

### P1 — signal 模式 ICE 候选会丢失（真实缺陷，直接造成"连不上/连得慢"）

`SignalConnection` 无消息缓冲（signal.ts），注册监听前到达的消息被静默丢弃：

- **被叫侧**（call.ts `joinSignalCall`）：`signal.onMessage`（处理 ice）在收到 offer **之后**才注册。主叫 trickle ICE 在 offer 之前到达的候选全部丢失，且 ICE 候选不会重传 → 候选缺失可致建连失败。
- **主叫侧**（`createSignalCall`）：onMessage 已注册，但对端的 `ice` 消息若先于 `answer` 到达，会对未 setRemoteDescription 的 pc 调 `addIceCandidate` → 抛异常且 async 回调无人捕获 → 候选丢失。

**修复**：两端各加候选缓冲队列——`setRemoteDescription` 完成前把 ice 消息存数组，完成后按序 `addIceCandidate`；`addIceCandidate` 加 catch 记诊断日志。

### P2 — signal 模式没有"挂断"信令

signal.go 只处理 `create/join/signal` 三种 action。一方挂断后对端只能等 ICE `disconnected` 3 秒兜底。参考 mirotalk 的 `peerLeft`：加 `bye` action 转发给房内对端，前端收到即 `endCall('对方已挂断', false)`。

### P3 — 状态一致性小问题

- `createSignalCall` / `joinSignalCall` 未调用 `lanSetBusy(true)`，信令通话中可能被局域网来电打断（前端 role 判断可兜底，建议补齐）。
- call.ts 82–84、601–610 行 tab/空格混用，建议统一。

### 做得好的

SAS 核对码设计、剪贴板邀请码自动检测、媒体降级（视频→语音→只收）、诊断日志覆盖、非 trickle ICE 的 gatheringComplete 兜底。

## 4. 参考项目结论

| | mirotalk | tl-rtc-file |
|---|---|---|
| 信令 | 自部署 Node + Socket.io | 自部署 Node + Socket.io（+可选 MySQL/coturn） |
| 无自己服务器？ | ❌（但其官方 demo `p2p.mirotalk.com` 是一台免费公共信令） | ❌（官方 demo `im.iamtsm.cn` 同理） |
| 体验借鉴 | 6 位房间码/链接，进房即通；peerLeft 处理离开 | 房间号+口令；WebRTC 检测面板 |

**结论：两个参考项目都强依赖信令服务器，照抄架构解决不了"没有服务器"的问题。** 能借鉴的是产品体验（房间码一段式接通、离开通知），而不是部署形态。

## 5. 无信令服务器方案（按推荐度）

### 方案 A（推荐）：内置"公共 Nostr 中继"信令模式

- 前端 WebSocket 直连 2–3 个免费公共 Nostr relay（如 `wss://relay.damus.io`、`wss://nos.lol`），以 6 位房间码派生订阅 filter，交换 offer/answer/ICE（可加对称加密）。可参考 Trystero 的 nostr 策略（MIT），也可自写 ~150 行 relay 客户端避免引依赖。
- **为什么可行**：信令只搬运 SDP/ICE 文本，第三方中继可见也不破坏机密性（媒体仍端到端 DTLS-SRTP），身份防伪由现有 SAS 核对码兜底——架构上零新增风险。
- **体验**：与 mirotalk 对齐——主叫生成房间码，微信发过去，对方输入即通，告别两段式邀请码。
- **合规性**：纯前端改动，不引新运行时/框架；若引 Trystero 需评估 bundle（约 +40KB gzip，可接受）；C7 要求接入诊断日志。

### 方案 B（零代码改动，今天就能用）：`signal` 子命令 + 免费隧道

发起方本机跑 `mini-vedio.exe signal`，用 `cloudflared tunnel --url http://localhost:<port>` 获得免注册的 `*.trycloudflare.com` 地址，对方在设置页填入该地址+房间码。复用现有全部代码，适合固定搭档的两人。

### 方案 C（小改动）：优化现有邀请码体验

主叫界面增加"等待对方回复码"的进度与剪贴板轮询检测（当前仅启动时检测一次），粘贴对方回复码后自动提交。保留离线场景可用性，但不解决"来回两次"的根本痛点。

**建议路线**：先落 P1/P2 修复（signal 模式本就不稳），同时实现方案 A 作为默认第四种模式，方案 B 作为 README 里的应急指引。
