# mini-vedio 设计缺陷与风险审查

审查时间：2026-09-05　范围：全部 Go 源码、`internal/lan`、前端 `src/`、构建脚本、AGENTS.md/README.md
验证动作：`go vet ./...` 通过（0 告警）、`CGO_ENABLED=0` 已确认、exe 实测 12,842,496 B（12.84 MB）

---

## P0 — 严重（安全机制或可用性失效）

### P0-1 代码尚未纳入版本控制
- **位置**：仓库根目录（`fatal: not a git repository`）
- **现象**：`.gitignore` 已就绪，但从未 `git init`。M0–M4 全部成果（约 30 个源文件 + 构建流水线）零历史、零分支、零回滚能力。
- **风险**：E 盘仅剩 3.4 GB，一次误删或构建脚本异常即全量丢失。AGENTS.md §11 自己写明"首次提交前先 git init"。
- **建议**：立即 `git init` + 首次提交，并把 `frontend/dist/`、`bin/`、`build/**/icon.ico` 的生成关系写进 README（当前 `.gitignore` 已忽略 `dist/` 与 `bin/`，合理）。

### P0-2 缺少 WebView2 运行时检测
- **位置**：`main.go`（全文无相关代码）
- **现象**：AGENTS.md §13 风险登记簿承诺"启动时检测，提示微软官方 Evergreen 离线安装器"，实现为零。
- **风险**：目标机器无 WebView2 时，Wails 初始化失败 → `log.Fatal` 在 `-H=windowsgui` 下无控制台可写 → **进程静默退出，用户双击后毫无反应**。这是 C1"双击即用"承诺的直接崩塌。
- **建议**：`main()` 开头用 `golang.org/x/sys/windows` 读 `HKLM\SOFTWARE\WOW6432Node\Microsoft\EdgeUpdate\Clients\{F3017226-FE2A-4295-8BDF-00C3A9A7E4C5}` 的 `pv` 值；缺失时调 `MessageBox` 提示并打开官方下载页。另建议把所有 `log.Fatal` 换成「MessageBox + 退出」，否则任何启动期错误都是静默失败。

### P0-3 TURN 不可配置 → 对称 NAT 必然失败
- **位置**：`frontend/src/services/call.ts:20` 的 `ICE_SERVERS` 常量（硬编码两个公共 STUN）；全仓库无 `settings.go`
- **现象**：AGENTS.md §5.1 要求"默认 STUN（**设置页可改**）"、§13 要求"设置页可配 TURN；未配置时明确报错"，**两者均未实现**。配置存储 `settings.json`（§6 隐私条款提到的文件）根本不存在。
- **风险**：
  1. M-5（对称 NAT / 企业网）**不可能通过验收**——没有 TURN 就没有穿透路径；
  2. 失败提示只有 `phaseDetail = 'P2P 连接失败，请检查网络后重新发起'`（`call.ts:117`），缺少 §5.1 强制要求的"可能被 AP 隔离或对称 NAT，改用邀请码或配置 TURN"分类文案；
  3. 无外网环境（纯内网）下 STUN 查询会徒增 ICE 收集耗时。
- **建议**：新增设置页 + `settings.json` 持久化（STUN 列表 / TURN url+username+credential / 设备名 / 端口），并把 ICE 失败按 `iceconnectionstate` 细分提示。注意这条会吃掉体积预算，见 P1-1。

### P0-4 SAS 防护静默降级
- **位置**：`sas.ts:11`（指纹提取失败返回 `''`）、`ConnectionStatus.vue:33`（`v-if="call.phase === 'connected' && call.sasCode"`）
- **现象**：若任一端 SDP 中正则未命中 `a=fingerprint`，`computeSas` 返回空串，整个 SAS 按钮**连同一并消失**。UI 只显示"已连接"，用户完全不知道防中间人校验已失效。
- **风险**：安全机制从"默认开启"退化为"成功才显示、失败当没发生"。这是本次审查中最典型的**安全静默降级**模式。
- **建议**：
  1. SAS 计算失败时显示显式的红色"无法验证加密，请谨慎通话"状态，而不是隐藏；
  2. 正则改为**提取全部** fingerprint 行并校验一致（当前只取第一个匹配，多 m= section 场景下会漏）；
  3. 当前 SAS 只有 3 字节（24 bit），业界（ZRTP）惯例是 32 bit，建议提到 4 字节；
  4. `sasVerified` 是用户单点切换、无任何强制（`ConnectionStatus.vue:37` 直接取反）——建议至少在通话前 30 秒高亮提示，弱化"安全剧场"成分。

### P0-5 局域网信令零认证
- **位置**：`internal/lan/lan.go` 的 `handle()`（L451）
- **现象**：UDP 报文只有 `App`/`V` 两个字段做合法性校验，**无签名、无 MAC、无来源 IP 一致性检查**。同网段任何进程都可以：
  1. 伪造 `announce` → `upsertPeer` 会用**报文里的 SigPort + 报文源 IP** 覆盖已存在 peer 的地址（L523-525），实现 **ID 劫持**，把后续 offer 引到自己这里；
  2. 伪造 `offer`（伪造 `ID`/`Name`）→ 目标机器弹出来电窗口（社交工程/骚扰）；
  3. 伪造 `bye` → 只要猜到 `callId` 即可挂断他人正在进行的通话。
- **风险**：SAS 理论上兜底，但 P0-4 已说明 SAS 会静默失效，两者叠加后局域网模式实际无任何身份保证。
- **建议**：
  - 短期：peer 的 sig 地址**首次登记后锁定**，不随后续 announce 变更（或仅在源 IP 相同时才更新）；陌生 `ID` 首次来电给二次确认；`bye`/`decline` 校验源 IP 必须等于该 peer 的登记 IP。
  - 中期：announce 携带设备公钥，信令消息带 Ed25519 签名（纯标准库 `crypto/ed25519`，体积影响可忽略）。

---

## P1 — 功能与体验（不符合既定验收标准）

### P1-1 体积余量仅 160 KB
- **实测**：`bin/mini-vedio.exe` = 12,842,496 B = **12.84 MB**
- **问题**：
  1. C2 上限 13 MB → **余量 1.2%**，P0-3（设置页）与 P1-6（signal 中继）任一落地都会立刻爆断言；
  2. AGENTS.md §8.2 清单里写的是"体积断言通过：构建产物 **≤ 12 MB**"，与 C2 的 13 MB **自相矛盾**；
  3. §8.3 体积记录表仍停在 11.4 MB，实际已涨 1.4 MB 且未登记，违反 §12 DoD 第 6 条"文档与实现不同步 = 任务未完成"；
  4. README badge 写 `~12MB`，与实测偏差 7%。
- **建议**：统一断言为 13 MB 并更新 §8.2/§8.3/README；优先用 `-gcflags=all=-l` / 裁剪未用 Wails 服务降体积，再谈加功能。

### P1-2 局域网呼叫被 4 秒 ICE 收集兜底拖慢
- **位置**：`call.ts:135-149` `gatheringComplete`（4s 超时兜底）、`startLanCall`（L406 调用它后再 `SendOffer`）
- **问题**：局域网场景本可用 host 候选立即直连，却与跨网共用非 trickle 流程。最坏情况下**点呼叫后要等 4 秒才发出 offer**，直接违背 M2 验收标准"3 秒内互见并可通话"。
- **建议**：LAN 模式改走 trickle ICE（`onicecandidate` 逐条经 `SendOffer` 增量发送），或至少把 LAN 分支的兜底超时降到 800 ms（host 候选通常 100 ms 内收齐）。

### P1-3 邀请码解压缩无大小上限（zip bomb）
- **位置**：`invite.ts:34-42` `inflate()`
- **问题**：对完全不可信的输入调用 `DecompressionStream` 且**无任何输出长度限制**。攻击者发一个几百字节的邀请码，可解压出 GB 级数据 → 浏览器内存耗尽。
- **建议**：分块读取解压流并累计字节数，超过阈值（如 256 KB）立即中止并报"邀请码格式不正确"。同时给 `decodeInvite` 加输入长度上限。

### P1-4 decodeInvite 未捕获 atob 异常
- **位置**：`invite.ts:16` `base64urlToBytes` → `atob(padded)`
- **问题**：粘贴含非 base64url 字符的文本时 `atob` 抛 `InvalidCharacterError`，`decodeInvite` 未 try/catch。**调用方**（`JoinRoom.vue:17`、`VideoRoom` 的 `submitAnswer`）都写了 catch，所以不会白屏，但错误信息会退化为"邀请码格式不正确"——属可接受降级，不过依赖调用方兜底是脆弱设计。
- **建议**：在 `base64urlToBytes` 内捕获并转成统一的中文错误。

### P1-5 通话计时器未实现
- **位置**：`VideoRoom.vue` / `CallControls.vue` 均无相关代码
- **问题**：M2 验收标准明确列了"通话 UI（静音/开关摄像头/挂断/**时长**）"，实现缺失。
- **建议**：在 `call.ts` 增加 `startedAt` + `duration` computed，`phase === 'connected'` 时启动 1s ticker。

### P1-6 signal 中继模式未实现
- **位置**：全仓库无引用
- **问题**：AGENTS.md §4.1 选型表把 `github.com/coder/websocket` 列为"WebSocket 库（仅模式③用）"，但实测**代码中零引用**——它是 Wails 自身的间接依赖（`go.mod` 标为 `// indirect`）。模式③（同一 exe 加 `signal` 子命令做转发服务器）从未落地，对称 NAT 场景因此**没有任何兜底**（与 P0-3 叠加）。文档此处存在误导，需修正。
- **建议**：要么实现（需新增子命令 + 前端接入，体积与工作量都要评估），要么在 AGENTS.md §4.1 明确标注"模式③未实现，coder/websocket 系 Wails 间接依赖"。

### P1-7 来电互相覆盖 + 无振铃音
- **位置**：`call.ts:366-368`（`lanIncoming.value = ev.data ?? null`）
- **问题**：
  1. 第二个来电会**直接覆盖**第一个，被覆盖的呼叫方毫不知情，会一直重试到 30 秒超时（`offerMaxRetries=15 × 2s`）；
  2. 来电只有视觉弹窗，**无振铃音**——人不在屏幕前就接不到。
- **建议**：改为来电队列（或"占线中"提示 + 挂起），加 `<audio>` 振铃（内嵌 base64 短音，几乎不增体积）。

---

## P2 — 健壮性与文档

### P2-1 `answers` / `offers` 集合永不清理
- **位置**：`lan.go:117`、`lan.go:119`；写入点 `AcceptCall`(L278)、`handleOffer`(L505)
- **问题**：两个 map 只增不删，`prunePeers` 也不碰它们。长跑后持续泄漏；更实际的是**同一 callId 被永久记住**——来电被拒绝后，若对方复用 callId 再呼，`handleOffer` 因 `offers[callId]` 已存在而直接 return（L501-504），**来电被永久吞掉**。
- **建议**：通话结束 / peer 下线 / 超时时清理对应 callId 的缓存条目。

### P2-2 `s.hub` 无 nil 保护且无同步
- **位置**：`lanservice.go:66`（`Peers`）、`lanservice.go:92`（`SetBusy`）、`lanservice.go:97`（`SendOffer`）… 全部直接解引用 `s.hub`
- **问题**：
  1. `s.hub` 在 `ServiceStartup`(L42) 才赋值，此前任何绑定调用 → **nil pointer panic**；
  2. 赋值与读取无 mutex/atomic 保护 → 真实 **data race**（建议用 `-race` 复现）。
- **建议**：改为 `atomic.Pointer[lan.Hub]` 或加 mutex，并在每个方法入口判空返回错误。

### P2-3 `readLoop` 出错即 continue，可能 CPU 忙循环
- **位置**：`lan.go:441`
- **问题**：`ReadFromUDP` 返回**非** `net.ErrClosed` 的错误时（如 `WSAECONNRESET`，Windows UDP 收到 ICMP port-unreachable 时会返回该错误）直接 `continue` → 若错误持续，goroutine 进入 100% CPU 忙循环。
- **建议**：对 `WSAECONNRESET` 类错误显式忽略（UDP 语义下正常），其他错误加退避 sleep 或计数熔断。

### P2-4 serve 自签证书带 CA 权限，私钥落盘
- **位置**：`serve.go:106-109`（`KeyUsageDigitalSignature | KeyUsageCertSign`、`IsCA: true`）、`serve.go:126`（写入 `serve-key.pem`）
- **问题**：一个纯 leaf 用途的自签服务器证书**完全不需要 CA 权限**，但这里签发的是可被当作 CA 使用的密钥，私钥以文件形式常驻 `%APPDATA%\mini-vedio\`。一旦被同用户的其他进程或备份同步读取，攻击者可用它签发任意域名的证书做中间人（浏览器不信任该根则无效，但风险不应主动制造）。
- **建议**：去掉 `KeyUsageCertSign` 与 `IsCA`；私钥改为**仅内存生成、不落盘**（牺牲"多次启动指纹一致"的提示价值，换来不留密钥）。此外 `http.Server` 未设 `ReadHeaderTimeout` / `ReadTimeout`，存在 Slowloris 面；`ListenAndServeTLS` 无 graceful shutdown，Ctrl+C 直接中断。

### P2-5 文档与实现多处不一致
| 文档位置 | 说法 | 实际 |
|---|---|---|
| §8.2 体积清单 | 断言 ≤ 12 MB | C2 为 13 MB，实测 12.84 MB |
| §8.3 体积记录 | 11.4 MB | 12.84 MB，涨 1.4 MB 未登记 |
| §4.1 选型表 | coder/websocket 用于模式③ | 代码零引用，系 Wails 间接依赖 |
| §5.1 / §13 | STUN 可改、TURN 可配 | 均硬编码，无设置功能 |
| §13 | WebView2 启动时检测 | 未实现 |
| §9 Backlog | DataChannel 属"M4 之后勿提前实现" | 已实现且 README 已宣传 |
| README 构建章节 | 给出 `wails3 generate syso` 命令 | §8.1 明确"勿走 syso 路线"，与 build.ps1 的 go-winres 方案冲突 |
| §7 目录结构 | 未列 `console_windows.go` / `console_other.go` | 实际存在 |

### P2-6 其他零散项
- **设备 ID 仅 32 bit 随机**（`device.go:54`，4 字节）：可猜测、可碰撞，且与 P0-5 的身份伪造叠加。建议提到 16 字节。
- **邀请码无时效、含公网 IP**：邀请码含 srflx 候选，泄露即暴露本机公网 IP:端口（可被 DDoS）。README 只说"可能被陌生人呼叫"，未提 IP 泄露，建议补充说明并考虑加时间戳+过期。
- **`AddPeerByIP`(L215) 与 `broadcastAddrs`(L363) 硬编码 `DefaultPort`**：Hub 已支持 `port` 参数化，这两处写死会导致非默认端口时行为不一致。
- **`SetName` 用字符串拼接路径**（`lanservice.go:86`，`dir+"\\device.json"`）：应改用 `filepath.Join`。
- **屏幕共享不通知对端**（`call.ts:230`）：`replaceTrack` 后对端画面突变但无任何提示，属隐私 UX 缺陷；且以 recvonly 加入时找不到 video sender 会直接失败。
- **聊天无长度/频率限制**（`call.ts:74-89`）：对端可高频刷屏。
- **无单实例检查、无 `--version`**：重复双击开多窗口抢摄像头。
- **测试覆盖极薄**：仅 `internal/lan/lan_test.go` 1 个集成测试（108 行）。最易出错的 `sas.ts`（正则提取指纹）与 `invite.ts`（压缩/解压/边界）**零测试**。
- **`.playwright-cli/` 遗留目录**未清理。

---

## 建议修复顺序

1. `git init` + 首次提交（5 分钟，收益最大）
2. WebView2 检测 + `log.Fatal` 改 MessageBox（消除静默失败）
3. SAS 失败显式告警 + 提取全量 fingerprint（补上安全静默降级）
4. LAN 信令加来源 IP 校验（低成本挡住大部分伪造）
5. 统一体积断言口径并更新 §8.2/§8.3/README（先记账，再决定 TURN 是否还塞得下）
6. 清理 `answers`/`offers`、加 `s.hub` nil 保护与同步、`readLoop` 错误退避
7. LAN 呼叫改 trickle（把 4 秒降到亚秒，过 M2）
8. serve 证书去 CA 权限 + 私钥不落盘
9. 补 `sas.ts` / `invite.ts` 单元测试
